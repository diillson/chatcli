{
  "complete.root.storage": "Inventory of every local store and on-demand curation (dry run by default)",
  "complete.storage.prune": "List what the retention rules would remove now; add --apply to remove it",
  "complete.storage.apply": "Remove instead of simulating",
  "complete.storage.dry_run": "Simulate only (the default)",
  "help.command.storage": "What every local store holds, its retention policy and what would go now; prune applies it (dry run unless --apply).",
  "storage.usage": "Usage: /storage [store] | /storage prune [store] [--apply | --dry-run]",
  "storage.unknown_store": "Unknown store %q. Stores: %s",
  "storage.error": "storage: %s",
  "storage.title": "Local storage",
  "storage.title_dry_run": "Storage prune — dry run",
  "storage.title_applied": "Storage prune — applied",
  "storage.col.store": "store",
  "storage.col.files": "files",
  "storage.col.size": "size",
  "storage.col.policy": "policy",
  "storage.col.prunable": "would go now",
  "storage.col.removed": "removed",
  "storage.total": "Total on disk: %s",
  "storage.applied": "Removed %d item(s), %s freed.",
  "storage.hint_apply": "%d item(s) can go. Run /storage prune --apply to remove them (or chatcli storage prune --apply).",
  "storage.nothing": "Nothing to remove under the current policies.",
  "storage.protected": "protected",
  "storage.on_apply": "swept on apply",
  "storage.reason.ttl": "past TTL",
  "storage.reason.orphan": "orphaned",
  "storage.reason.burst": "test burst",
  "storage.reason.stray": "stray temp",
  "storage.policy.ttl": "session TTL",
  "storage.policy.orphan": "session TTL + orphaned workspace",
  "storage.policy.burst": "session TTL + test bursts on prune",
  "storage.policy.runs": "task-graph retention (30d)",
  "storage.policy.cap": "CCR TTL + size cap",
  "storage.policy.idle": "hub idle TTL",
  "storage.policy.machine": "session TTL, machine sessions only",
  "storage.policy.protected": "never pruned",
  "storage.cmd.usage": "Usage: chatcli storage [store] | chatcli storage prune [store] [--apply | --dry-run] [--json]",
  "cfg.kv.retention.checkpoints": "coder checkpoints",
  "cfg.kv.retention.checkpoints_note": "(and whenever the workspace no longer exists)",
  "cfg.retention.storage_hint": "/storage shows every store with its size and what the policies would remove now; /storage prune --apply runs them on demand."
}
