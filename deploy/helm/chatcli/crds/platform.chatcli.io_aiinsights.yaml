---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: aiinsights.
spec:
  group: ""
  names:
    kind: AIInsight
    listKind: AIInsightList
    plural: aiinsights
    shortNames:
    - ai
    singular: aiinsight
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.issueRef.name
      name: Issue
      type: string
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .status.confidence
      name: Confidence
      type: number
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: ""
    schema:
      openAPIV3Schema:
        description: AIInsight contains AI-generated analysis and recommendations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIInsightSpec defines the desired state of an AIInsight.
            properties:
              issueRef:
                description: IssueRef references the parent Issue.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              model:
                description: Model is the LLM model used for analysis.
                type: string
              provider:
                description: Provider is the LLM provider used for analysis.
                type: string
            required:
            - issueRef
            - model
            - provider
            type: object
          status:
            description: AIInsightStatus defines the observed state of an AIInsight.
            properties:
              analysis:
                description: Analysis is the AI-generated root cause analysis.
                type: string
              confidence:
                description: Confidence is the AI's confidence in its analysis (0.0-1.0).
                type: number
              generatedAt:
                description: GeneratedAt is when the analysis was generated.
                format: date-time
                type: string
              recommendations:
                description: Recommendations are AI-suggested actions (human-readable).
                items:
                  type: string
                type: array
              suggestedActions:
                description: SuggestedActions are concrete remediation actions suggested
                  by the AI.
                items:
                  description: SuggestedAction represents a concrete remediation action
                    suggested by the AI.
                  properties:
                    action:
                      description: Action is the remediation action type (ScaleDeployment,
                        RestartDeployment, RollbackDeployment, PatchConfig).
                      type: string
                    description:
                      description: Description explains why this action is recommended.
                      type: string
                    name:
                      description: Name is a human-readable step name.
                      type: string
                    params:
                      additionalProperties:
                        type: string
                      description: Params are action-specific parameters.
                      type: object
                  required:
                  - action
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
