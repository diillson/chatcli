---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterregistrations.
spec:
  group: ""
  names:
    kind: ClusterRegistration
    listKind: ClusterRegistrationList
    plural: clusterregistrations
    shortNames:
    - cr
    singular: clusterregistration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .status.connected
      name: Connected
      type: boolean
    - jsonPath: .status.nodeCount
      name: Nodes
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: ""
    schema:
      openAPIV3Schema:
        description: ClusterRegistration represents a registered Kubernetes cluster
          for multi-cluster federation.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRegistrationSpec defines the desired state of ClusterRegistration.
            properties:
              capabilities:
                description: Capabilities lists available cluster features (e.g.,
                  "metrics-server", "prometheus", "istio").
                items:
                  type: string
                type: array
              displayName:
                description: DisplayName is a human-readable name for this cluster.
                type: string
              environment:
                default: dev
                description: Environment classification (prod/staging/dev).
                enum:
                - prod
                - staging
                - dev
                type: string
              healthCheckInterval:
                default: 30s
                description: HealthCheckInterval defines how often to check cluster
                  connectivity (e.g., "30s", "1m").
                type: string
              kubeconfigSecretRef:
                description: KubeconfigSecretRef references a Secret containing the
                  kubeconfig for this cluster.
                properties:
                  name:
                    description: Name is the Secret name.
                    type: string
                required:
                - name
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels for custom metadata.
                type: object
              maxConcurrentRemediations:
                default: 3
                description: MaxConcurrentRemediations limits parallel remediations
                  in this cluster.
                format: int32
                type: integer
              region:
                description: Region where the cluster is deployed (e.g., "us-east-1",
                  "eu-west-1").
                type: string
              tier:
                default: standard
                description: Tier determines remediation policies (critical/standard/non-critical).
                enum:
                - critical
                - standard
                - non-critical
                type: string
            required:
            - displayName
            - environment
            - kubeconfigSecretRef
            - tier
            type: object
          status:
            description: ClusterRegistrationStatus defines the observed state of ClusterRegistration.
            properties:
              activeIssues:
                description: ActiveIssues across all namespaces in this cluster.
                format: int32
                type: integer
              activeRemediations:
                description: ActiveRemediations in this cluster.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connected:
                description: Connected indicates if the cluster is reachable.
                type: boolean
              kubernetesVersion:
                description: KubernetesVersion reported by the cluster.
                type: string
              lastHealthCheck:
                description: LastHealthCheck is when connectivity was last verified.
                format: date-time
                type: string
              namespaceCount:
                description: NamespaceCount in the cluster.
                format: int32
                type: integer
              nodeCount:
                description: NodeCount in the cluster.
                format: int32
                type: integer
            required:
            - activeIssues
            - activeRemediations
            - connected
            - namespaceCount
            - nodeCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
